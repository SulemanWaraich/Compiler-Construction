begin main {
    number x = 20;
    number y = 4;
    
    number sum = x + y;
    number diff = x - y;
    number product = x * y;
    number quotient = x / y;

    publish(sum);
    publish(diff);
    publish(product);
    publish(quotient);
}
